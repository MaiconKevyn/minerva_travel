{
  "name": "minerva-travel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "frontend_atual/apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web && rm -rf dist && cp -R frontend_atual/apps/web/dist ./dist",
    "start": "npm run start --workspace web",
    "lint": "npm run lint --workspace web",
    "test": "npm run test --workspace web"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.4",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
